{
  "version": 2,
  "builds": [
    {
      "src": "server/src/**/*.js",
      "use": "@vercel/node"
    },
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/src/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/client/$1"
    }
  ]
}

